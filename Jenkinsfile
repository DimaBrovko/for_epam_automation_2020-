pipeline {
    agent { dockerfile true }

    stages {
        stage('Test_login') {
            steps {
                echo "Test login successfully completed"
            }
            }
        }
    }
